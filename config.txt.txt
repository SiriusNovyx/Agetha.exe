# Agetha v4.2.0 (Mod) config — original author is @tomiszivacs github
# ─────────────────────────────────────────────────────────────────────────────

# ── AI Backend ────────────────────────────────────────────────────────────────

# Set to "yes" to use a local AI (Ollama) instead of Groq.
# If "no", Agetha uses Groq. Run `ollama list` to see installed models.
USE_LOCAL_AI = no

# Set to "no" to fully disable Groq even when USE_LOCAL_AI = no.
# (Useful if you only want to test without any API calls.)
ENABLE_GROQ = yes

# ── Groq API Keys ─────────────────────────────────────────────────────────────
# Add up to 10 keys (separate free-tier accounts) to rotate around rate limits.
GROQ_API_KEY = gsk_
GROQ_API_KEY_2 = 
GROQ_API_KEY_3 = 
GROQ_API_KEY_4 = 
GROQ_API_KEY_5 = 
GROQ_API_KEY_6 = 
GROQ_API_KEY_7 = 
GROQ_API_KEY_8 = 
GROQ_API_KEY_9 = 
GROQ_API_KEY_10 = 

# Groq model. Smaller/cheaper models have more forgiving rate limits.
GROQ_MODEL = llama-3.3-70b-versatile

# ── Local AI (Ollama) ─────────────────────────────────────────────────────────
# Only used when USE_LOCAL_AI = yes.
# If LOCAL_AI_MODEL is wrong or not installed, Agetha falls back to idle responses.
LOCAL_AI_MODEL = 
LOCAL_AI_TIMEOUT = 30

# ── OS Permissions ────────────────────────────────────────────────────────────
# Allow Agetha to run shell commands, open/create/delete files, and control
# other application windows (target_window_move / target_window_resize)?
ENABLE_COMMAND_EXECUTION = yes

# ── Context & Memory ─────────────────────────────────────────────────────────
# Characters of long-term memory to inject into each prompt.
# Higher = richer personality memory, higher token cost.
MEMORY_CHARS = 600

# Number of recent exchanges kept in short-term conversation history.
# Higher = longer context, higher token cost.
HISTORY_LIMIT = 6

# Maximum characters Agetha reads when you ask her to open a document.
# Higher = she understands more of the file, higher token cost.
FILE_READ_CHARS = 200

# ── Animation ─────────────────────────────────────────────────────────────────
# GIF playback speed multiplier. Lower = faster, higher = slower.
# 0.4 = snappy  |  0.6 = default  |  1.0 = original speed
ANIMATION_SPEED = 0.6
